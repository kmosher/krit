[package]
name = "krit-core"
description = "The krit wire contract and server discovery, shared by the server and its clients."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true
